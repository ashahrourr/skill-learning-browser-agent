Quick-apply instruction: use the requested apply flow directly.

- Do not require reading the full description or judging fit unless the user explicitly asked for that.
- Choose an eligible visible listing using visible listing details and the requested apply button/flow.
- If a search or filter field already contains the requested value, commit it with the site UI before assuming it is applied.
- If eligible visible listings already satisfy the requested apply flow, proceed with them instead of searching longer for a separate filter control.
- Do not audit every form field. Trust prefilled/autofilled profile fields, skip optional fields, and click non-final Next/Continue buttons so the website can tell you what is actually missing.
- Only fill fields that are empty and visibly required or fields the website marks invalid.
- Ask for approval only before the final submit button.
